"""
Events provides engine for event management
`Event` is base class for Event object
`EventDialog` is extension of `Dialog` class and it's base class for dialog in events object
`EventsEngine` is class provides dialog management
"""

from events.event import Event, EventDialog, EventsEngine
from events.eventsList import events_list
